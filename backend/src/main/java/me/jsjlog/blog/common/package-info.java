/**
 * 여러 도메인에서 공통으로 사용하는 기반 코드를 모아두는 패키지입니다.
 *
 * <p>특정 기능에 종속되지 않는 컨트롤러, 응답 포맷, 예외 처리, 유틸리티가 이 하위에 들어갑니다.
 * 예를 들어 profile, post, admin 기능이 모두 같은 API 응답 형식을 쓰기 때문에
 * response와 exception 패키지를 common 아래에 둡니다.</p>
 */
package me.jsjlog.blog.common;
