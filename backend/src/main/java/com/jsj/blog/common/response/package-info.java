/**
 * API 응답 DTO를 모아두는 패키지입니다.
 *
 * <p>성공 응답은 {@code ApiResponse}, 실패 응답은 {@code ErrorResponse}로 분리합니다.
 * 이 규칙을 지키면 프론트엔드에서 모든 API 응답을 같은 방식으로 처리할 수 있습니다.</p>
 */
package com.jsj.blog.common.response;
