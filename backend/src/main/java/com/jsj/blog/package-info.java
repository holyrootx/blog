/**
 * 블로그 백엔드 애플리케이션의 최상위 패키지입니다.
 *
 * <p>Spring Boot는 {@code com.jsj.blog} 아래 패키지를 기준으로 컴포넌트를 스캔합니다.
 * 앞으로 도메인이 늘어나면 {@code profile}, {@code post}, {@code admin}처럼 기능 단위 패키지를
 * 이 아래에 만들고, 여러 기능이 함께 쓰는 코드는 {@code common} 아래에 둡니다.</p>
 *
 * <p>패키지를 나누는 기본 기준은 "누가 이 코드를 주로 소유하는가"입니다.
 * 특정 화면이나 기능에만 필요한 코드는 해당 도메인 패키지에 두고,
 * 응답 포맷이나 예외 처리처럼 전체 API 규칙을 담당하는 코드는 공통 패키지로 분리합니다.</p>
 */
package com.jsj.blog;
